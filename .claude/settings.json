{
  "hooks": {
    "PostToolUse": [
      {
        "matcher": "Write|Edit",
        "hooks": [
          {
            "type": "command",
            "command": "jq -r '.tool_input.file_path // .tool_response.filePath' | { read -r f; [ -n \"$f\" ] && pnpm exec biome check --write \"$f\"; } 2>/dev/null || true"
          }
        ]
      }
    ]
  },
  "enabledPlugins": {}
}
